# Plan

## 目标

- 统一竞拍面板标题与倒计时标题表达
- 修正自定义出价面板标题来源

## 前置依赖

- 无跨仓依赖
- 以当前主路径面板口径为对齐基准

## 执行切片

### Slice 1

- 改动范围：普通出价面板
- 主要文件/模块：面板组装逻辑
- 预期产出：普通出价面板补齐倒计时标题
- 风险：空值语义不一致可能引发客户端表现差异

### Slice 2

- 改动范围：自定义出价面板
- 主要文件/模块：自定义面板标题与倒计时标题组装
- 预期产出：自定义面板使用自己的标题配置，并补齐倒计时标题
- 风险：如果仍复用普通面板标题，会继续存在错误文案

### Slice 3

- 改动范围：与主路径对齐验证
- 主要文件/模块：兼容路径输出核对
- 预期产出：不同返回路径下 UI 文案口径一致
- 风险：只改字段不做对齐验证，后续仍可能存在细微漂移

## 顺序与并行关系

- Slice 1 和 Slice 2 可并行
- Slice 3 必须在前两步完成后执行
- 关键路径是自定义面板标题源修正

## 验证计划

- 最小验证：`go test ./entities/loaders/product_loaders`
- 重点确认：两个面板都能返回倒计时标题
- 重点确认：自定义面板标题不再复用普通面板标题

## 回滚与兜底

- 若任一面板字段出现兼容问题，可分切片回滚
- 优先保留当前主路径不动，回滚兼容路径补齐
- 不需要额外降级开关
