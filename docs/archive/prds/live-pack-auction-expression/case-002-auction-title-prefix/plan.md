# Plan

## 目标

- 在命中实验时，为 regular auction 标题补充 `Auction` 标识
- 保证标题前缀异常时可以平滑回退，不影响原有标题展示

## 前置依赖

- 依赖现有实验字段可读取
- 依赖现有本地化文案能力可用
- 不涉及协议、TCC 结构或发布窗口改动

## 执行切片

### Slice 1

- 改动范围：共享前缀文案 key 与分隔符
- 主要文件/模块：`auction_converters/const.go`
- 预期产出：统一维护标题前缀和连接符
- 风险：连接符不稳定会影响标题可读性

### Slice 2

- 改动范围：regular auction 标题组装逻辑
- 主要文件/模块：`regular_auction_converter`
- 预期产出：实验命中时补充 `Auction` 前缀
- 风险：若未做空值回退，可能出现空标题片段

## 顺序与并行关系

- 先确定前缀文案 key 和连接符
- 再补标题组装逻辑
- 关键路径是实验命中条件和空值回退逻辑

## 验证计划

- 最小验证：`go test ./entities/converters/auction_converters`
- 重点确认：实验命中时标题增加 `Auction` 标识
- 回归确认：实验未命中或文案为空时，标题仍为原标题

## 回滚与兜底

- 若标题显示异常，可直接回滚前缀拼接逻辑
- 保留原有标题来源与组装方式
- 不需要额外降级开关
